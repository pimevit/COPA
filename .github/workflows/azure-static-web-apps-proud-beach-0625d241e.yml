name: Frontend - Azure Static Web App (proud-beach)

on:
  push:
    branches:
      - master
    paths:
      - 'frontend/**'
      - '.github/workflows/azure-static-web-apps-proud-beach-0625d241e.yml'
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - master
    paths:
      - 'frontend/**'
      - '.github/workflows/azure-static-web-apps-proud-beach-0625d241e.yml'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: frontend-azure-static-webapp-proud-beach
  cancel-in-progress: false

env:
  NODE_VERSION: '24.x'
  APP_LOCATION: frontend
  DIST_LOCATION: frontend/dist

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and deploy frontend
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: ${{ env.APP_LOCATION }}

      - name: Test frontend
        run: npm test
        working-directory: ${{ env.APP_LOCATION }}

      - name: Validate production API URL
        if: github.event_name != 'pull_request'
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
        run: |
          if [ -z "$VITE_API_BASE_URL" ]; then
            echo "Repository variable VITE_API_BASE_URL must point to the backend App Service URL." >&2
            exit 1
          fi

      - name: Build frontend
        run: npm run build
        working-directory: ${{ env.APP_LOCATION }}
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}

      - name: Validate built frontend output
        run: |
          if [ ! -f "${DIST_LOCATION}/index.html" ]; then
            echo "Expected ${DIST_LOCATION}/index.html to exist after the frontend build." >&2
            exit 1
          fi

          if [ ! -f "${DIST_LOCATION}/staticwebapp.config.json" ]; then
            echo "Expected ${DIST_LOCATION}/staticwebapp.config.json to be copied from frontend/public." >&2
            exit 1
          fi

          if grep -q '/src/main.tsx' "${DIST_LOCATION}/index.html"; then
            echo "Built index.html still points to Vite source files instead of compiled assets." >&2
            exit 1
          fi

          if ! find "${DIST_LOCATION}/assets" -maxdepth 1 -type f -name '*.js' | grep -q .; then
            echo "Expected at least one compiled JavaScript asset in ${DIST_LOCATION}/assets." >&2
            exit 1
          fi

      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PROUD_BEACH_0625D241E }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: ${{ env.DIST_LOCATION }}
          api_location: ''
          output_location: ''
          skip_app_build: true

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close pull request

    steps:
      - name: Close pull request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PROUD_BEACH_0625D241E }}
          action: close
