# ⚠️ DEPRECATED - Use .github/workflows/frontend-azure-static-webapp.yml instead
# This workflow has been disabled because it was missing the build step
# causing the "dist directory not found" error.
